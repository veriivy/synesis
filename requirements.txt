# k2mod.py / orchestrator talk to IFM/K2 through the OpenAI-compatible chat API.
openai>=1.60
python-dotenv>=1.0

# Claude advocate (a1). Native Messages API — not OpenAI-compatible enough
# to share the IFM client.
anthropic>=0.40

# Long-running FastAPI process. The K2 call takes tens of seconds; Vercel
# functions time out, so this stays a Python server (Vultr later).
fastapi>=0.115
uvicorn>=0.32

# Typed FinalPlan/Ticket validation (orchestrator/schemas.py) — the ticket
# decomposition + disjoint-ownership validator slice.
pydantic>=2.6

# Tests. Run `pytest` from the repo root (orchestrator/ has no __init__-less
# top level, so imports resolve as `orchestrator.xxx`).
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
